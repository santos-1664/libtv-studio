# 安全说明

更新日期：2026-09-16

## 部署边界

LibTV Studio 面向个人或可信团队的单工作区。所有获准访问的人共同使用项目、素材、模型服务和配置；当前没有多租户或细粒度角色隔离。请只向可信使用者开放，不要让不可信用户修改服务设置。

Node 与 Docker Compose 默认仅供本机访问。需要公网访问时：

- 设置强 `ACCESS_PASSWORD`，通过 HTTPS 反向代理访问。
- 将 `APP_ORIGIN` 设为浏览器实际访问的完整 origin，并设置 `COOKIE_SECURE=true`。本地 HTTP 使用 `COOKIE_SECURE=false`。
- 保持应用端口仅供反向代理访问，避免绕过代理直接连接应用。
- 使用自己有权限的模型服务与 API Key，并在供应商侧配置合适的额度。

服务 URL 决定请求及密钥的发送位置。更换文本、图片或视频服务的 origin（协议、域名或端口）时，需要重新输入目标服务的 API Key；不要依赖空值保留旧服务密钥。

## 凭据和数据

仓库不包含 API Key、数据库、主密钥或个人素材。运行时的模型密钥在服务端加密保存，设置页不回填已存密钥。保护 `data/`、Docker 数据卷、`.env` 和备份；这些内容不应提交到 Git 或附在公开问题报告中。

备份前停止应用，并同时保留数据库、媒体及 `.master-key`。如果设置了 `SETTINGS_ENCRYPTION_KEY`，还需要保存相同的密钥。丢失解密密钥会影响已有服务配置的恢复。发现 API Key 泄露时，应先在供应商处撤销或轮换它，再更新应用配置。

## 报告安全问题

请勿在公开 Issue、Pull Request 或评论中粘贴密钥、会话 Cookie、数据库、私密素材或可直接攻击他人部署的细节。

如果仓库已启用 GitHub Private Vulnerability Reporting，可从 **Security → Report a vulnerability** 私密提交。若看不到入口，请先联系维护者确认私密接收渠道；尚无联系方式时，可以公开询问报告渠道，但先不要披露漏洞细节或附件。

报告可包含受影响的提交或版本、部署方式、预期与实际行为，以及使用临时数据的最小复现步骤。日志需先移除凭据和私人内容。维护者确认后再协调修复和公开说明。

当前优先维护默认分支，尚未承诺旧版本的独立安全维护周期。
