# 验收记录

更新日期：2026-09-16。此版本为不携带个人配置或历史内容的发布源码包。

## 开发阶段验证

以下记录来自功能开发阶段，不表示发布包含有已配置的模型或历史生成成果。

| 验收项 | 开发阶段结果 |
| --- | --- |
| 自动测试 | 44 项通过，覆盖 SQLite、项目与素材归属、访问保护、凭据加密、OpenAI 兼容 URL、Bearer 鉴权、流式工具调用、旧配置处理、跨来源密钥保护、任务与数据恢复 |
| 类型检查与构建 | TypeScript 类型检查和 Vite 生产构建通过 |
| 配置与模型目录 | 文本和 Agent 使用 API URL、API Key、模型配置；成功读取实际模型目录；设置页不回填密钥明文 |
| 文本、图片与视频 | 已在开发环境完成真实生成和归档，结果可以在节点显示、预览或播放 |
| Agent 与局部修改 | 完成脚本、分镜和引用连接创建；将第二镜头修改为远景并重新生成，对应节点外的内容保持一致 |
| 持久化 | 保存后刷新能恢复节点内容、位置和连接；容器重建保留数据卷中的数据 |

## 敏感信息清理后的验证

发布材料仅保留 `data/.gitkeep`，不提供数据库、旧主密钥、API Key、OA token、任务配置快照、签名下载链接、会话或个人素材。首次启动自动创建 SQLite 表与新的加密主密钥。

清理后重新执行并通过以下检查，均使用独立空数据卷，不导入开发环境配置：

- 44 项自动测试全部通过；TypeScript、Vite 和 Docker 镜像构建通过。
- 首次初始化返回 `empty-seed`；自动创建 8 张表、空配置和新的 32 字节主密钥，文件权限为 `0600`，主应用以 UID 1000 运行。
- 首次项目列表为空，文本、图片、视频三个模型状态均为未配置，设置中的服务地址为空。
- 新建项目并保存文本、图片、视频节点及画布视图；容器重启后内容、位置和视图一致。
- 三种节点生成与 Agent 调用在未配置时返回明确的 503 提示，不创建生成任务或不完整对话。
- 发布工作树与解密得到的原凭据、旧主密钥、专属域名及个人记录标识进行比对，未发现匹配；另作独立只读复核。

本轮未重新调用付费模型。

## 开源发布准备（2026-09-16）

- 统一文本、图片、视频配置的密钥来源校验；切换 origin 且未提供新 Key 时拒绝整个修改，原配置保持不变。
- 增加 6 项媒体配置回归测试；完整 50 项测试全部通过，类型检查与生产构建通过。
- 使用本地假服务验证拒绝修改后不会向新来源发送原 Key，未调用真实或付费模型。
- 添加 MIT 许可证、运行依赖许可全文、安全说明、贡献指南及非官方项目和资源说明。
- 发布工作树再次与 94 项已知敏感值比对，无命中；data 仍仅有 .gitkeep。

## 范围与限制

真实生成需要部署者自行配置有效的 API URL、API Key 与具有对应能力的模型。图片编辑及首帧图生视频实现经过本地协议测试；不同兼容服务的鉴权、模型和工具调用能力仍需对应服务实际验证。

系统面向单工作区，未包含视频内容理解、第三方单点登录、时间线剪辑、成片拼接、多人协作或多租户权限体系。下载针对单个素材。自动测试的模拟服务仅用于验证代码，不作为产品生成结果。
