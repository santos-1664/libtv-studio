# 贡献指南

欢迎提交缺陷修复、功能改进和文档完善。较大的交互或架构变化可先开 Issue 说明问题、目标行为和实现范围。

## 本地开发

使用 Node.js 22.16 或更新的 22.x 版本：

```bash
npm ci
git switch -c codex/your-change
npm run dev
```

未配置模型服务时，也可开发项目管理、画布编辑、上传和预览。真实模型验证需要自己的 API URL、API Key 和额度；自动测试使用本地模拟服务，不需要真实密钥。

## 提交 Pull Request

1. 从默认分支创建 `codex/` 开头的工作分支，围绕一个明确问题修改。
2. 保持 TypeScript 类型及前后端数据契约一致；修复行为缺陷时补充有针对性的回归测试。
3. 提交前运行适用的检查：

```bash
npm test
npm run build
docker compose config --quiet
```

4. Pull Request 说明触发问题的条件、修改后的行为和实际执行的验证。界面改动可附使用临时内容的截图；纯文档修改注明已检查链接及命令即可。

不要将未执行的真实模型验证写为通过。需要调用付费服务的检查，应明确其作用与费用，并由执行者自行配置和授权。

## 数据与资源

- 不提交 `.env`、API Key、Cookie、数据库、主密钥、运行日志、个人项目、会话或素材；`data/` 只保留空目录占位。
- 使用最小化、非敏感的测试样本，避免把真实服务响应或个人工作区复制进测试。
- 新增图片、图标、字体、模板或其他资源时，记录来源、适用许可及必要署名；更新 [资源与品牌说明](docs/资源与品牌说明.md) 或第三方声明。
- 只提交你有权贡献的代码与资源，并遵循仓库的 [许可证](LICENSE)。本项目的独立实现与品牌关系见 [README](README.md)。

安全问题请按 [安全说明](SECURITY.md) 私密报告，不要在公开 Issue 提交敏感复现材料。
