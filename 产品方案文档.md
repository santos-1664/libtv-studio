# LibTV Studio 产品方案

版本：1.0.0  
日期：2026-09-16  
交付形式：无凭据、无历史数据的源码交付

## 1. 产品目标

LibTV Studio 面向个人视频创作者，参照 LibTV 的核心交互，将首页与画布创作、项目管理、Agent 三个模块连接为同一套创作流程。项目组织脚本、提示词、图片、视频和引用关系，支持手动编辑与 Agent 操作，内容可保存并继续创作。

本交付包含完整前后端工程、数据库初始化逻辑和 Docker 部署配置。发布包不包含服务凭据、数据库、主密钥、个人项目、会话、上传素材、生成结果或原始验收数据；`data/` 仅有 `.gitkeep`。接收人首次启动后获得空白工作区，并自行配置模型服务。

## 2. 考核要求对应

| 维度 | 权重 | 实现 |
| --- | --- | --- |
| 功能覆盖度 | 50 分 | 首页与节点画布、项目管理、Agent 交互；支持脚本、分镜、媒体生成、指定节点修改、预览和保存恢复。 |
| 生产可用性 | 30 分 | React 前端、Express 后端、SQLite 自动建表、Dockerfile、Compose、任务持久化、素材归档、访问保护和凭据加密。 |
| 交付物 | 20 分 | 无凭据源码 ZIP、本文档；飞书会议录屏由提交人录制并另附可访问的妙记链接。 |

本项目聚焦三核心模块，不包含参照商业产品的所有功能；实现与验证范围以交付文档为准。

## 3. 功能范围

### 首页与画布

首页提供空白项目、文本/图片/视频创作、提示模板、最近项目及 Agent 入口。提示模板和界面插画是产品资源，不是个人生成的样例作品。

画布支持文本、图片、视频节点；拖动、缩放、连接引用；名称、提示词、模型、比例和时长编辑；复制、删除、撤销、重做；上传素材、预览结果和下载原文件。模型列表按节点类型读取，未配置服务时说明所需配置。

自动保存使用版本校验，并提供浏览器草稿恢复。并发更新时保留本地修改供用户合并；生成任务结果并入画布时保留正在编辑的提示词和位置。上传素材可从当前项目素材库重新加入画布。

### 项目页

项目页支持创建、搜索、网格/列表视图、更新时间或名称排序、重命名、删除和重新打开。项目卡片显示名称、封面、节点数量及更新时间。

项目是画布、素材、会话和任务的归属单位。服务端禁止跨项目引用素材；生成中的项目受到删除保护。首次启动无历史项目，接收人创建的内容才会出现在列表中。

### Agent

Agent 提供 Skill 分类、搜索、收藏、模型选择、附件、项目会话切换和草稿恢复。回复通过 SSE 流式展示，工具操作以卡片显示，并提供画布入口。

内置 Skill 覆盖分镜导演、商品广告、画面灵感、脚本编剧、镜头修改和提示词优化。受控工具可创建节点、修改指定节点、建立引用、提交生成任务及重命名项目；服务端校验参数和项目归属，并限制每轮处理次数。

## 4. 创作闭环

```text
启动空白工作区 → 配置自己的模型服务
                  ↓
新建项目 → 输入需求 / 选择 Skill / 上传自己的素材
                  ↓
Agent 创建脚本与分镜 → 画布编辑节点及引用
                  ↓
提交生成 → 任务状态更新 → 结果写回并归档
                  ↓
预览、播放、下载 → 项目页重新打开继续编辑
```

手动操作和 Agent 使用同一份项目数据。局部修改通过节点 ID 或镜头序号定位，支持只修改指定镜头并重新生成，而不重写整个项目。

开发阶段曾用真实模型验证脚本与分镜创建、指定镜头修改和重新生成，并比较其他节点及连接保持一致。该阶段的作品、会话、媒体和原始验收数据不随源码交付。

## 5. 技术架构

```text
React + TypeScript + React Flow
首页 / 项目页 / 节点画布 / Agent / 设置
                  ↓ HTTP API / SSE
Express + Node.js
项目 / 素材 / 画布保存 / Agent 工具 / 生成任务
          ↙                         ↘
SQLite + 本地媒体目录            模型适配层
                           OpenAI 兼容文本 / 百炼媒体
```

| 层次 | 职责 |
| --- | --- |
| 前端 | 页面导航、节点编辑、草稿恢复、冲突提示、媒体预览、流式回复与状态展示。 |
| API 与校验 | 参数校验、工作区会话、来源检查、上传和项目归属检查。 |
| SQLite | 保存接收人后续创建的项目、画布、素材元数据、任务、消息和加密配置；首次运行自动建表。 |
| 媒体存储 | 保存用户上传及生成的素材；供应商临时结果下载归档后由本地接口提供访问。 |
| 模型适配层 | OpenAI 兼容聊天与工具调用、百炼图片协议、视频异步任务，隔离各供应商请求差异。 |

SQLite 启用 WAL 与外键约束。画布保存使用版本号防止静默覆盖。生成任务先入库，记录输入、幂等键、执行阶段及外部任务 ID；相同节点避免重复创建进行中任务。

服务重启后，已取得外部视频任务 ID 的任务继续查询，已取得结果的任务可恢复下载。无法确认是否受理的请求保持不确定状态，核实供应商记录后再解除重试保护。

模型密钥由服务端加密保存，状态接口不回传已保存的明文密钥。源码包不附带加密配置或解密主密钥；新部署首次启动生成自己的本地主密钥。应用提供工作区密码和 HttpOnly 会话 Cookie。

## 6. 模型服务配置

文本与 Agent 需要 API URL、API Key、默认模型，对应 `TEXT_BASE_URL`、`TEXT_API_KEY`、`TEXT_MODEL`。设置接口使用 `textBaseUrl`、`textKey`、`textModel`。

文本根地址自动补 `/v1`；显式填写 `/v1`、`/compatible-mode/v1` 等路径时保留原路径。服务端追加 `/models` 和 `/chat/completions`。百炼图片与视频使用接收人自己的业务空间根地址及原生媒体协议。

首次配置必须提供有效密钥，之后密钥留空保存会保留同一部署已保存的值。模型列表读取不提交生成，模型是否支持聊天、工具调用或媒体生成需按实际服务确认。

百炼媒体适配器包含 Qwen-Image 3.0 和 Wan 2.7。视频节点采用已支持的默认 Wan 文生视频系列、模型留空且连接首帧时，使用配套首帧模型；显式选择文生视频却连接图片时显示冲突提示。视频时长为 2–15 秒，图生视频比例跟随一张有效首帧。

无配置时可使用项目管理、画布编辑和本地素材操作。真实文本、图片、视频生成及 Agent 对话必须由接收人先配置服务，并依赖其账户权限与额度。

## 7. 部署与数据管理

### Docker

```bash
docker compose up --build -d
```

访问 [本机工作台](http://localhost:3100)。源码包的运行目录为空，首次在空数据卷启动时自动建立数据库及新主密钥。应用使用非 root 用户运行，数据保存在独立的 `studio-data` 数据卷，已有数据不会被初始化流程覆盖。

### Node.js

使用 Node.js 22.16 或更新的 22.x 版本：

```bash
npm ci
npm run build
npm start
```

访问 [本地工作台](http://127.0.0.1:3100)。可选使用无密钥的 `.env.example` 模板配置端口或访问密码。模型服务需要在设置页自行配置。

开始创作后，接收人备份自己的部署时应同时保留数据库、媒体和对应主密钥。首次空白初始化与恢复自己已有备份是不同操作；本源码包不提供开发者的历史备份或个人内容。

## 8. 验证记录与边界

| 验证项 | 范围 |
| --- | --- |
| 自动测试 | 开发阶段 44 项通过，覆盖持久化、参数校验、访问保护、模型协议、SSE 和任务恢复等。 |
| 构建 | 开发阶段 TypeScript、Vite 和 Docker 镜像构建通过。 |
| 真实模型能力 | 开发阶段验证了文本、图片、视频，以及 Agent 分镜创建、局部修改与重新生成。 |
| 交付数据 | 不附带运行数据库、凭据、主密钥、个人项目、会话、上传素材、生成结果或原始验收数据。 |
| 脱敏后验收 | 本文不声称清理后又执行了真实模型或付费生成；源码检查的实际结果见 `docs/验收记录.md`。 |

开发阶段验证说明实现曾经工作，不代表接收人无需配置即可使用同一账户或生成结果。测试模拟服务只用于自动测试，不用于产品生成。

当前不包含视频内容理解、第三方单点登录、完整时间线剪辑、成片拼接、多人协作和多租户权限体系。下载能力针对单个素材。

## 9. 交付清单

| 交付物 | 内容 |
| --- | --- |
| 项目源码 ZIP | 前后端源码、依赖锁文件、测试、Dockerfile、Compose、初始化与运行脚本、无密钥配置模板、使用文档和空运行目录。 |
| 产品方案文档 | 产品目标、三模块功能、架构、配置、部署与验证范围。 |
| 飞书妙记链接 | 提交人使用飞书会议录制真实操作后单独提供，并确认评委可访问。 |

录屏前需在录制环境配置自己的服务并准备新项目。录制可展示空白项目、手动画布、Agent 分镜与局部修改、生成状态、播放下载和保存恢复；不得声称包内包含历史个人作品或已配置服务。
